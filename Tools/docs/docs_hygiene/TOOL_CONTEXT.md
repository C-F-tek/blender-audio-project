# Tools/docs/docs_hygiene context

`Tools/docs/docs_hygiene` contains documentation maintenance packages.

Main areas:

```text
inventory
audit
module duplication
legacy invocation rewrite
package move/promote
Markdown split/coherence
```

Use the dispatcher names from `Tools/docs/dispatch.py`.

Generated reports should remain local unless intentionally promoted as compact evidence.
