# AI command surface

`ia_carmine` is a compatibility command and import surface only.

The IA-Carmine Core Runtime lives in `ia_carmine.*`; use
`python -m ia_carmine.cli <tool> [args...]` for canonical runtime commands.
