# Tools/ai/repository_product context

This package contains repository-level report builders and summary helpers.

Use commands from `Tools/ai/dispatch.py` through:

```powershell
python -m Tools.ai <tool> [args...]
```

Keep generated runtime output out of versioned documentation unless it is compact evidence.
