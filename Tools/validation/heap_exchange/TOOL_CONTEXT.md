# Tools/validation/heap_exchange context

Validation package for heap exchange lifecycle and related smoke checks.

Use public commands through:

```powershell
python -m Tools.validation <tool> [args...]
```

Public validation names are registered in `Tools/validation/dispatch.py`.
