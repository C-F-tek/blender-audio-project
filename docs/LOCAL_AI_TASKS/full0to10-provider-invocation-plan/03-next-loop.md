# Next loop

Il prossimo loop deve collegare il provider invocation plan al bundle finale:

```text
dry-run plan
bundle inclusion
contract gate
real-run flag proposal
```

La generation resta vietata finché la real-run non viene invocata in modo
esplicito.
