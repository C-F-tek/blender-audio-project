# Next loop

Il prossimo loop deve collegare il bridge alla promozione bundle:

```text
execution bridge
bundle promotion gate
artifact completeness
provider real-run flag proposal
```

La run reale resta separata e richiede comando esplicito.
