# Codex with Ollama bootstrap

This branch is a lightweight test PR target for validating the Codex/Ollama local workflow.

Scope:

- keep branch based on current `master`;
- use it as a clean local target for Codex/Ollama experiments;
- avoid committing runtime artifacts such as `output/**`, DB files, renders, and generated chunk indexes.

No runtime validation has been executed by this bootstrap commit.
