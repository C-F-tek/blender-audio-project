# Full0To10 provider governor

Il provider governor decide se una lane provider può ricevere un permit.

## Output

- governor JSON;
- run permit JSON;
- telemetry JSON;
- Markdown report.

## Nessuna generation

Il governor non chiama provider generativi. Produce solo permessi, budget e
policy verificabili.
