# Next god loop

Il prossimo loop deve collegare il permit a una run simulata controllata:

```text
permit
-> dry-run provider invocation plan
-> expected telemetry contract
-> workload report contract
-> bundle inclusion
```

Ancora nessuna generation automatica. Prima si simula il piano, poi si abilita
la run reale con consenso esplicito.
