# Discovery

Il resolver cerca file JSON in:

- output;
- input;
- inputs;
- data;
- assets;
- Scripting.

## Pattern

- `analysis.json`, `_analysis.json`;
- `music_context.json`, `_music_context.json`;
- `blender_keyframes.json`, `_keyframes.json`.
