# Final Purpose Composition - Apply Package Detail - 2026-05-14

## Purpose

This piece expands the operator decision into package-level apply details. It is
intended for review before staging or committing.

## Package A - Proposal Gate

Status: implemented in the current working tree.

Files:

- `Tools/ai/heap_final_proposals/cli.py`
- `Tools/ai/_shared/heap_proposal_gate.py`
- `Tools/validation/test_proposal_gate.py`

Implementation detail:

- Composer imports deterministic gate helpers.
- Raw proposal chunks are gated before final packaging.
- `operator_decision` is written into JSON.
- `Operator decision` is rendered into Markdown.
- `OPERATOR_DECISION.txt` is exported into the Documents package.
- Smoke tests cover fake target rejection, repeated rejection and accepted
  patchable target behavior.

Apply decision:

- Include this package first.
- Do not include output artifacts generated by the heap run.
- Track the new helper and smoke test if the operator accepts the gate.

## Package B - Wrapper And Link Fix

Status: now implemented in the working tree.

Files:

- `Tools/ai/heap_context_closure/cli.py`
- `docs/LOCAL_AI_RUN_BOOTSTRAP.md/README.md`
- `docs/LOCAL_AI_RUN_BOOTSTRAP.md/part-001.md`
- `docs/LOCAL_AI_RUN_BOOTSTRAP.md/_ia_carmine_md_split_manifest.json`

Implementation detail:

- The heap wrapper accepts `--request-file`.
- A UTF-8 request file overrides inline `--request`.
- The launcher report records `operator_request_file`.
- The heap chunk contract now tells GPU1 it may re-open and enrich prior chunks.
- The bootstrap split container now has one real part and no dead link to
  missing `part-002.md`.

Apply decision:

- Include Package B with Package A or immediately after it.
- This fixes a real launch fragility and a deterministic docs-link failure.

## Package C - Provider Re-Iteration Prompt

Status: implemented as a narrow prompt and loop hardening edit.

File:

- `Tools/ai/heap_runtime/completeness_gate/cli.py`

Implementation detail:

- The GPU1 provider prompt now explicitly allows refinement of existing proposal
  chunks when they are valid but thin.
- The prompt asks each revision to identify whether it extends a previous chunk
  or creates a new delta.
- The provider revision loop now emits terminal `NO_PATCHABLE_TARGET` feedback
  when a repeated fake path survives deterministic feedback.

Apply decision:

- Include this with Package B if the operator wants the next heap run to consume
  the richer composition protocol immediately.

## Package D - Runtime Anti-Fake-Path Hardening

Status: partly implemented; deeper fixture coverage remains.

Target:

- `Tools/ai/heap_runtime/completeness_gate/cli.py`
- A focused validator under `Tools/validation/`.

Needed next work:

- Add a fake provider response fixture.
- Move the terminal-loop detector into a smaller helper when splitting the
  oversized runtime file.

Apply decision:

- Keep separate from A/B/C because the runtime file is 4785 lines and should be
  touched carefully.

## Package D2 - Heap Code Execution Matrix

Status: implemented and validated.

Files:

- `Tools/ai/_shared/heap_code_execution_tool_core.py`
- `Tools/ai/heap_runtime/code_execution_tool/cli.py`
- `Tools/validation/run_heap_code_execution_tool_smoke.py`
- `Tools/ai/agent_runtime_tool_broker.py`
- `Tools/ai/heap_runtime/completeness_gate/cli.py`

Implementation detail:

- Added broker tool `run_heap_code_execution_matrix`.
- The tool generates a guarded debug-lab request, runs compile/test/diff
  evidence and emits `concrete_code_proposals`.
- Untracked new files are captured as `developed_change_present` with source
  excerpts, so code proposals are concrete even before staging.
- Coding requests now require `code_execution_matrix` before a ready heap
  product can be accepted.

Apply decision:

- Include this package in the current apply set.
- Treat generated `output/**` reports as evidence only, not source.

## Package E - Composer Decomposition

Status: proposed, not implemented.

Targets:

- `Tools/ai/heap_final_package_writer.py`
- `Tools/ai/heap_final_report_collectors.py`
- `Tools/ai/heap_final_proposals/cli.py`

Needed next work:

- Move package writing into a helper.
- Move proposal/provider collection into a helper.
- Reduce composer line count without changing behavior.

Apply decision:

- Do after A/B/C are accepted and validated.

## Package F - Documentation Hygiene

Status: proposed, not implemented beyond the bootstrap link fix.

Targets:

- Missing mandatory-doc references.
- Oversized maintained Markdown.
- Unindexed or prune-candidate docs.

Apply decision:

- Dedicated docs pass only.
- Use repository Markdown tools where possible.

## Package G - `.claude-tools`

Status: decision needed.

Current file:

- `.claude-tools/repo_toolbox.py`

Recommendation:

- Do not include it in the first apply.
- Either keep local-only or promote later into a maintained `Tools/*` owner with
  README and smoke validation.
